# Bundle deploy state (local, per-target)
.databricks/

# Ingestion staging output before upload to the UC Volume
.staging/

# Python
__pycache__/
*.pyc
.venv/

# Secrets (should never exist here — env vars or a secrets manager only — but belt and braces)
.env

# Web demo (web/) — Node
node_modules/
web/**/dist/
web/server/.env
web/server/.env.1password
